Q1: What happens when you initialize a repository? why do
you need to do it?
A: a file called .git will be added to the directory, that
contains metadata about the repository. 
I need to to it, so git creates a new repository that i 
can use in the future to commit changes.

Q2: How is the staging area different from the working directory
and the repository? What value do you think it offers?
A: the staging area is a conncetion between the working directory
and the repository, if we want i file to be tracked we need to
add it to the staging area. otherwise it wont be trakced.

Q3: How can you use the staging area to make sure you have one
commit per logical change?
A: by commiting sparate files that each has a logical change.

Q4: What are some situations when branches would be helpful
in keeping your history organized? How would branches help?
A: when adding new experimental feature, or when trying to change
some old stuff and see what happenes. Branches would help by
organizing changes with separate comits.

Q5: How do the diagrams help you visualize the branch structure?
A: by drawing a tree diagrams that represents that branch
structure.

Q6: What is the result of merging two branches together?
Why do we represent it in the diagram the way we do?
A: the result is a combination between the two branches,
because the 2 braches are being combained to one branch.

